<!DOCTYPE html>
<html>
<head>
  <title>HemingwayRailsApp</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div id="container">
  <h1>Creativity with Style</h1>

    <h3>
      <% if logged_in? %>
    <%= "Logged in as #{current_user.username}" %>
    <%= link_to "Logout", logout_path %>
    <% else %>
    <%= link_to "Login", login_path %>
    <% end %>
    <!-- <%= link_to "Home", ideas_path %> -->
  </h3>
  <h3>
  <%= link_to "Add to The Scene", new_bar_path, class: "new_bar" %>
  <%= link_to "The Writers", creators_path %>
  <%= link_to "The Scene", bars_path %>
  <%= link_to "The Word", ideas_path %>
  <a href="https://www.google.com/maps">Google Maps</a>
</h3>
</div>
  <%= yield %>
</body>
</html>

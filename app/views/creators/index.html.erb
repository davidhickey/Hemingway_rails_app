<body>
  <div id="content">
<p id="notice"><%= notice %></p>

<h1>The Writers</h1>

<table>
  <thead>
    <tr>
      <th>Username</th>
      <th>Email</th>
      <!-- <th>Password</th>
      <th>Password confirmation</th> -->
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @creators.each do |creator| %>
      <tr>
        <td><%= creator.username %></td>
        <td><%= creator.email %></td>
        <!-- <td><%= creator.password %></td>
        <td><%= creator.password_confirmation %></td> -->
        <td><%= link_to 'Show', creator %></td>
        <td><%= link_to 'Edit', edit_creator_path(creator) %></td>
        <!-- <td><%= link_to 'Destroy', creator, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Writer', new_creator_path %>
</div>
</body>

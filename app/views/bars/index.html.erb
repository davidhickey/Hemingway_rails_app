<h1>All Bars</h1>

<ul id="bars">
  <% @bars.each do |bar| %>
    <li>
      <%= link_to bar.name, bar_path(bar) %>
    </li>
  <% end %>
</ul>

<%= link_to "Add a New Bars", new_bar_path, class: "new_bar" %>
